{
  "name": "googleplaces",
  "version": "1.0.0",
  "description": "A web app (desktop & mobile) that provides a query box and a search button and then calls the Places Library for Google Maps ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Satish Chandra Palkaoti",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
